<%= form_with(model: consumption, local: true) do |form| %>
  <% if consumption.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(consumption.errors.count, "error") %> prohibited this consumption from being saved:</h2>

      <ul>
      <% consumption.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="row">
      <div class="col s12 m6 offset-m3">
        <div class="card white lighten-3">
          <div class="card-content black-text">
            <span class="card-title black-text">Úprava spotřeby</span>

            <div class="field">
              <%= form.label :value %>
              <%= form.text_field :value, id: :consumption_value %>
            </div>

            <a class="waves-effect waves-light btn">
              <%= form.submit 'upravit' %>
            </a>
          </div>
        </div>
      </div>
    </div>
<% end %>
