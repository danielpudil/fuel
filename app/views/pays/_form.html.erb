<%= form_with(model: pay, local: true) do |form| %>
  <% if pay.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pay.errors.count, "error") %> prohibited this pay from being saved:</h2>

      <ul>
      <% pay.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="row">
      <div class="col s12 m6 offset-m3">
        <div class="card white lighten-3">
          <div class="card-content white-text">
            <span class="card-title black-text">Vytvoření platby</span>

            <div class="field">
              <%= form.label :price %>
              <%= form.number_field :price, id: :pay_price, class: 'black-text' %>
            </div>

            <a class="waves-effect waves-light btn">
              <%= form.submit 'Zaplatit' %>
            </a>
          </div>
        </div>
      </div>
    </div>
<% end %>
